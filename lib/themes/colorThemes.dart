import 'package:flutter/material.dart';

class ColorThemes {
  // 主要字体颜色
  static const fColor = Color.fromRGBO(103, 99, 105, 1);

  // tab select
  static const BTSelectColor = Color.fromRGBO(241, 238, 232, 1);

  // top bar
  static const ABColor = Color.fromRGBO(214, 211, 205, 1);

  // 卡片背景色
  static const cardBDColor = Color.fromRGBO(241, 238, 232, 1);

  // img 背景色
  static const imgBDColor = Color.fromRGBO(220, 217, 218, 1);
}
